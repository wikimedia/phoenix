{
  "query": "query Page($authority: String!, $name: String!) { pageByName(authority: $authority, name: $name) { name dateModified hasPart about { key val } } }",
  "variables": { "authority": "en.wikipedia.org", "name": "Foobar" }
}
